{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "sphere.schema.json",
    "title": "Sphere",
    "type": "object",
    "description": "An implicit sphere centered at the local space origin.",
    "allOf": [
        {
            "$ref": "glTFProperty.schema.json"
        }
    ],
    "properties": {
        "radius": {
            "type": "number",
            "description": "The radius of the sphere in local space.",
            "minimum": 0
        },
        "slice": {
            "$ref": "sphere.slice.schema.json",
            "description": "The optionally-defined slice of the sphere."
        }
    },
    "required": [
        "radius"  
    ]
}
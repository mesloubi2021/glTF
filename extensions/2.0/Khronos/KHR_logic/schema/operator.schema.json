{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "operator",
    "type": "object",
    "description": "Operator definition.",
    "allOf": [ { "$ref": "glTFChildOfRootProperty.schema.json" } ],
    "properties": {
        "operand": {
            "default": "VALUE",
            "description": "ToDo: Add all operands.",
            "anyOf": [
                {
                    "const": "IDENTITY"
                },
                {
                    "const": "NOT"
                },
                {
                    "const": "AND"
                },
                {
                    "const": "OR"
                },
                {
                    "const": "+"
                },
                {
                    "const": "-"
                },
                {
                    "type": "string"
                }
            ]
        },
        "type": {
            "default": "number",
            "description": "Allowed types.",
            "anyOf": [
                {
                    "const": "number"
                },
                {
                    "const": "integer"
                },
                {
                    "const": "boolean"
                },
                {
                    "type": "string"
                }
            ]
        },
        "defaultValues": {
            "type": "array",
            "description": "The defaulr values of this operand.",
            "items": {
                "oneOf": [
                    {
                        "type": "number"
                    },
                    {
                        "type": "integer"
                    },
                    {
                        "type": "boolean"
                    }
                ]
            },
            "minItems": 1
        },
        "inputOperators": {
            "type": "array",
            "description": "The indices of this operator's input operators. If not present, this operand is using the default values.",
            "items": {
                "$ref": "glTFid.schema.json"
            },
            "minItems": 1
        },
        "name": { },
        "extensions": { },
        "extras": { }
    },
    "required": [
        "operand",
        "type",
        "defaultValues"
    ]
}

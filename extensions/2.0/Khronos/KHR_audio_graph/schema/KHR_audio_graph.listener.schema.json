{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "listener.schema.json",
    "title": "KHR_audio_graph Listener",
    "type": "object",
    "description": "Describes the position and other physical characteristics of a listener from which the audio output of spatial emitter nodes is heard when spatial audio processing is used. For most applications it makes the most sense to attach the listener node to the main camera..",
    "properties": {
        "id": {
            "description": "A unique identifier of the audio listener in the scene.",
            "type": "integer"
        },
        "extensions": {},
        "extras": {}
    }
}
